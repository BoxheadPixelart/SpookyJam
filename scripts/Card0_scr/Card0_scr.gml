///create code 
//interval 
//create cod
Accent = Card1_snd; 
if (CanSpawn = true and CurrentNote = 1) {
	CanSpawn = false; 
	alarm[0] = quarter
	a1 = instance_create_depth(x,y,10,Arrow_obj)
	a1.image_blend = c_red; 
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}

if (CanSpawn = true and CurrentNote = 2) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}

if (CanSpawn = true and CurrentNote = 3) {
	CanSpawn = false; 
	alarm[0] = dottedquarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 4) {
	CanSpawn = false; 
	alarm[0] = eighth
		instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 5) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 6) {
	CanSpawn = false; 
	alarm[0] = half
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 7) {
	CanSpawn = false; 
	alarm[0] = eighth
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 8) {
	CanSpawn = false; 
	alarm[0] = eighth
	instance_create_depth(x,y,10,Arrow_obj)
	audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 9) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 10) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 11) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 12) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 13) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}if (CanSpawn = true and CurrentNote = 14) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 15) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
//	audio_play_sound(Beep,0,false);
	CurrentNote += 1; 
}
if (CanSpawn = true and CurrentNote = 16) {
	CanSpawn = false; 
	alarm[0] = quarter
	instance_create_depth(x,y,10,Arrow_obj)
	//audio_play_sound(Beep,0,false);
	CurrentNote = 1; 
	Play = false;
}